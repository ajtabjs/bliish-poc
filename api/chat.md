# sending messages on the global chat

the chat uses the api endpoint `/chat/messages`, with an additional parameter passed through the site to define which group to send the message in

such an example url would be `https://bliish.com/api/v1/chat/messages?group=memez`, with the group parameter being for the memes group on bliish

